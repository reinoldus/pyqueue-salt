List all connected minions:
salt-key -L

Accept new minions:
salt-key -A

Remove minion:
salt-key -d [hostname]

Hostname:
* - all hosts
single hosts can be seen with salt-key -L

salt '[hostname]' state

Execute single state:
salt '[hostname]' state.sls [statename]
